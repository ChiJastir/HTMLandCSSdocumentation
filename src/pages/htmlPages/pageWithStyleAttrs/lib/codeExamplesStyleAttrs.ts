export const useCodeExamplesStyleAttrs = () => {
    const classDiv = `<div class="container"></div>`
    const idDiv = `<div id="container"></div>`
    const styleDiv = `<div style="color: red;"></div>`

    return { classDiv, idDiv, styleDiv }
}